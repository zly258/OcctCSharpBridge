# OcctSelectionMode

- **Assembly:** `OcctNet.dll`
- **Namespace:** `OcctNet`

## Declaration

```csharp
public enum OcctSelectionMode
```

## Description

Public API type. See its declaration, member signatures, and conceptual documentation for ownership, lifetime, and behavioral constraints.

## Fields / Enum Values

- `Edge` — `OcctSelectionMode` = 2 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Face` — `OcctSelectionMode` = 4 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Object` — `OcctSelectionMode` = 0 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Shell` — `OcctSelectionMode` = 5 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Solid` — `OcctSelectionMode` = 6 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Vertex` — `OcctSelectionMode` = 1 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Wire` — `OcctSelectionMode` = 3 — Public API member. Exact parameters, return type, and available XML documentation are listed below.

