# OcctAssemblyNodeKind

- **Assembly:** `OcctNet.dll`
- **Namespace:** `OcctNet`

## Declaration

```csharp
public enum OcctAssemblyNodeKind
```

## Description

Semantic role of a node in an imported XDE assembly occurrence tree.

## Fields / Enum Values

- `Assembly` — `OcctAssemblyNodeKind` = 0 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Instance` — `OcctAssemblyNodeKind` = 1 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Part` — `OcctAssemblyNodeKind` = 2 — Public API member. Exact parameters, return type, and available XML documentation are listed below.

