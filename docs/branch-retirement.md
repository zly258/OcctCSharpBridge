# Retired Standalone Branches

After migration, `avalonia` and `avalonia-dev` are no longer supported branches. Their retained functionality lives in `main` SDK adapters and the unified `demo` consumer.
