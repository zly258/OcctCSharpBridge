# 01 快速开始

本项目面向 **Windows x64 + .NET 10 + Open CASCADE Technology 7.9.0**。核心程序集 `OcctNet` 提供两个主要入口：

- `OcctModelingSession`：无窗口建模、拓扑、算法、分析、网格和数据交换；
- `OcctEngine`：AIS/Viewer、对象显示、选择、相机、外观、标注和交互。

WinForms、WPF、Avalonia 只是在不同 UI 框架中承载 `OcctEngine` 的可复用 Viewport Host。

## 1. 环境要求

```text
Windows x64
.NET SDK 10.0.302
Visual Studio 2022 / MSVC
CMake 3.21+
Open CASCADE Technology 7.9.0
```

仓库默认 OCCT 根目录：

```text
D:\tools\occt-vc144-64
```

也可以通过 `-OcctRoot` 或 `OCCT_ROOT` 指定其它位置。

## 2. 先验证 Managed 层

不需要安装 OCCT SDK：

```powershell
.\build.ps1 validate Release
.\build.ps1 managed Release
```

`validate` 负责静态契约检查；`managed` 构建四个 Managed 项目并运行不依赖 OCCT 的回归验证。

## 3. 构建完整 Bridge

默认 OCCT 路径存在时：

```powershell
.\build.ps1 all Release
```

其它路径：

```powershell
.\build.ps1 all Release -OcctRoot "E:\SDK\occt-7.9.0"
```

完整 Native 验证：

```powershell
.\build.ps1 smoke Release -OcctRoot "E:\SDK\occt-7.9.0"
```

只有 Native Smoke 能真正证明 C++ 编译、链接、DLL 加载以及 OCCT 几何/拓扑执行正常。

## 4. 构建并引用 NuGet

```powershell
.\build.ps1 pack Release
```

输出：

```text
artifacts\packages
```

核心包：

```powershell
dotnet add package OcctNet --version 2.6.0 --source .\artifacts\packages
```

按实际 UI 技术再添加一个 Host，例如 WPF：

```powershell
dotnet add package OcctNet.Wpf --version 2.6.0 --source .\artifacts\packages
```

Managed 包不捆绑 `OcctNative.dll` 和 OCCT `TK*.dll`。运行应用时必须部署匹配的 Native Bridge 与 OCCT Runtime，详见 [07 运行时部署与诊断](07_运行时部署与诊断.md)。

## 5. Headless 建模

```csharp
using OcctNet;

using var model = new OcctModelingSession();

var plate = model.MakeBox(100, 80, 10);
var hole = model.MakeCylinder(
    new OcctPoint3d(50, 40, -5),
    OcctVector3d.UnitZ,
    8,
    20);

var cut = model.Cut(plate, hole);
var bounds = model.GetShapeBounds(cut.Shape);
var inertia = model.GetVolumeInertiaProperties(cut.Shape);
var inspection = model.InspectShape(cut.Shape);

model.ExportStep(cut.Shape, "plate.step");
```

`OcctModelShape` 与创建它的 `OcctModelingSession` 绑定。不要保存裸 ID 后在另一个 Session 中直接复用。

## 6. Viewer

直接使用 `OcctEngine` 时，需要有效的原生窗口句柄：

```csharp
using var engine = new OcctEngine();
engine.Initialize(hwnd);
engine.SetView(OcctViewOrientation.Isometric);
engine.SetProjection(OcctProjectionType.Orthographic);
engine.FitAll();
```

桌面应用通常直接使用对应 Host：

```text
WinForms  -> OcctViewportControl
WPF       -> OcctWpfViewport
Avalonia  -> OcctAvaloniaViewport
```

`OcctNet.Avalonia` 当前仍通过 Windows HWND 承载 Viewer，因此它是 Windows Host，并不表示 Native Viewer 已支持 Linux/macOS。

## 7. 将 Headless Shape 显示到 Viewer

```csharp
using var model = new OcctModelingSession();
var shape = model.MakeBox(100, 80, 60);

using var engine = new OcctEngine();
engine.Initialize(hwnd);

var displayed = engine.Display(model, shape, fit: true);
```

`displayed` 是属于目标 `OcctEngine` 的 Viewer 对象；原始 `OcctModelShape` 仍属于 Modeling Session。

## 8. Runtime 显式配置

开发机没有采用 app-local Runtime 时，可以在创建第一个 Engine/Session 前配置：

```csharp
OcctRuntime.Configure(
    occtRoot: @"D:\tools\occt-vc144-64",
    nativeBridgeDirectory: @"D:\workspace\OcctCSharpBridge\build\native\bin\Release");
```

出现 DLL 加载问题时先输出：

```csharp
Console.WriteLine(OcctRuntime.GetDiagnosticReport());
```

## 9. 下一步阅读

- 需要理解 `main` 为什么不包含 Document/Command/Tool：阅读 [02 架构与边界](02_架构与边界.md)。
- 需要查当前能力范围：阅读 [03 API 覆盖与设计约定](03_API覆盖与设计约定.md)。
- 需要进行几何、拓扑、History、Topology Reference：阅读 [04 几何建模与拓扑分析](04_几何建模与拓扑分析.md)。
- 需要 Viewer 选择与交互：阅读 [05 Viewer 选择与交互](05_Viewer选择与交互.md)。
