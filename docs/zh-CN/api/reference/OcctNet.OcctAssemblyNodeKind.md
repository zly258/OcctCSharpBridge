# OcctAssemblyNodeKind

- **程序集:** `OcctNet.dll`
- **命名空间:** `OcctNet`

## 声明

```csharp
public enum OcctAssemblyNodeKind
```

## 说明

Semantic role of a node in an imported XDE assembly occurrence tree.

## 字段 / 枚举值

- `Assembly` — `OcctAssemblyNodeKind` = 0 — 公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。
- `Instance` — `OcctAssemblyNodeKind` = 1 — 公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。
- `Part` — `OcctAssemblyNodeKind` = 2 — 公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

