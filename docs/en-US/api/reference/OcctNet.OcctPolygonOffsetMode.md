# OcctPolygonOffsetMode

- **Assembly:** `OcctNet.dll`
- **Namespace:** `OcctNet`

## Declaration

```csharp
public enum OcctPolygonOffsetMode
```

## Description

Public API type. See its declaration, member signatures, and conceptual documentation for ownership, lifetime, and behavioral constraints.

## Fields / Enum Values

- `All` — `OcctPolygonOffsetMode` = 7 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Fill` — `OcctPolygonOffsetMode` = 1 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Line` — `OcctPolygonOffsetMode` = 2 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Off` — `OcctPolygonOffsetMode` = 0 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Point` — `OcctPolygonOffsetMode` = 4 — Public API member. Exact parameters, return type, and available XML documentation are listed below.

