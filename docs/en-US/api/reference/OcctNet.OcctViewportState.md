# OcctViewportState

- **Assembly:** `OcctNet.dll`
- **Namespace:** `OcctNet`
- **Inheritance:** `ValueType`

## Declaration

```csharp
public struct OcctViewportState
```

## Description

Public API type. See its declaration, member signatures, and conceptual documentation for ownership, lifetime, and behavioral constraints.

## Constructors

None

## Properties

### `AntialiasingEnabled`

Public API member. Exact parameters, return type, and available XML documentation are listed below.

```csharp
public bool AntialiasingEnabled { get; }
```

### `AutomaticHighlight`

Public API member. Exact parameters, return type, and available XML documentation are listed below.

```csharp
public bool AutomaticHighlight { get; }
```

### `ComputedMode`

Public API member. Exact parameters, return type, and available XML documentation are listed below.

```csharp
public bool ComputedMode { get; }
```

### `FaceBoundariesVisible`

Public API member. Exact parameters, return type, and available XML documentation are listed below.

```csharp
public bool FaceBoundariesVisible { get; }
```

### `FrustumCullingEnabled`

Public API member. Exact parameters, return type, and available XML documentation are listed below.

```csharp
public bool FrustumCullingEnabled { get; }
```

### `ShadowsEnabled`

Public API member. Exact parameters, return type, and available XML documentation are listed below.

```csharp
public bool ShadowsEnabled { get; }
```

## Events

None

## Methods

None

## Fields / Enum Values

- `Height` — `int` — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `MsaaSamples` — `int` — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `PerspectiveFieldOfView` — `double` — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `ProjectionType` — `OcctProjectionType` — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `RenderResolutionDpi` — `double` — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `RenderResolutionScale` — `double` — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `RenderingMethod` — `OcctRenderingMethod` — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `SelectionTolerance` — `int` — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Width` — `int` — Public API member. Exact parameters, return type, and available XML documentation are listed below.

