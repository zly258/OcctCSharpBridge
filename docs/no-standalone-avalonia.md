# Avalonia Branch Retirement

Avalonia remains supported in the SDK and Demo. Only the standalone `avalonia` and `avalonia-dev` branches are retired.

Windows uses Avalonia alongside WinForms and WPF. Linux uses Avalonia as its only Demo host.
