# OcctModelState

- **Assembly:** `OcctNet.dll`
- **Namespace:** `OcctNet`

## Declaration

```csharp
public enum OcctModelState
```

## Description

Public API type. See its declaration, member signatures, and conceptual documentation for ownership, lifetime, and behavioral constraints.

## Fields / Enum Values

- `Inside` — `OcctModelState` = 1 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `On` — `OcctModelState` = 3 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Outside` — `OcctModelState` = 2 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Unknown` — `OcctModelState` = 0 — Public API member. Exact parameters, return type, and available XML documentation are listed below.

