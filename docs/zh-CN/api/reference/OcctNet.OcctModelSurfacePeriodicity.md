# OcctModelSurfacePeriodicity

- **程序集:** `OcctNet.dll`
- **命名空间:** `OcctNet`
- **继承:** `ValueType`

## 声明

```csharp
public struct OcctModelSurfacePeriodicity
```

## 说明

公开 API 类型。具体语义、所有权和生命周期约束以类型声明、成员签名及对应专题文档为准。

## 构造函数

### `OcctModelSurfacePeriodicity`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public OcctModelSurfacePeriodicity(bool IsUClosed, bool IsVClosed, bool IsUPeriodic, bool IsVPeriodic, double UPeriod, double VPeriod)
```

**参数**

- `IsUClosed` — `bool`
- `IsVClosed` — `bool`
- `IsUPeriodic` — `bool`
- `IsVPeriodic` — `bool`
- `UPeriod` — `double`
- `VPeriod` — `double`

## 属性

### `IsUClosed`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public bool IsUClosed { get; set; }
```

### `IsUPeriodic`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public bool IsUPeriodic { get; set; }
```

### `IsVClosed`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public bool IsVClosed { get; set; }
```

### `IsVPeriodic`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public bool IsVPeriodic { get; set; }
```

### `UPeriod`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public double UPeriod { get; set; }
```

### `VPeriod`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public double VPeriod { get; set; }
```

## 事件

无

## 方法

### `Deconstruct`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public void Deconstruct(out bool IsUClosed, out bool IsVClosed, out bool IsUPeriodic, out bool IsVPeriodic, out double UPeriod, out double VPeriod)
```

**参数**

- `IsUClosed` — `out bool`
- `IsVClosed` — `out bool`
- `IsUPeriodic` — `out bool`
- `IsVPeriodic` — `out bool`
- `UPeriod` — `out double`
- `VPeriod` — `out double`

**返回值:** `void`

### `Equals`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public bool Equals(OcctModelSurfacePeriodicity other)
```

**参数**

- `other` — `OcctModelSurfacePeriodicity`

**返回值:** `bool`

### `Equals`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public virtual bool Equals(object obj)
```

**参数**

- `obj` — `object`

**返回值:** `bool`

### `GetHashCode`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public virtual int GetHashCode()
```

**返回值:** `int`

### `ToString`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public virtual string ToString()
```

**返回值:** `string`

## 字段 / 枚举值

无

