# SDK Source of Truth

`main` is the only formal SDK source. Demo branches never vendor SDK implementation source or committed Binary SDK output.

The local `dist/` directory is disposable cache state validated by contract, manifest, source commit and SHA256.
