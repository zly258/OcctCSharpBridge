# 构建、测试与发布

## Windows

```powershell
.\build.ps1 build Release -OcctRoot "D:\tools\occt-vc144-64"
.\build.ps1 test Release
.\build.ps1 smoke Release -OcctRoot "D:\tools\occt-vc144-64"
```

验证层级只有：

1. ManagedTests；
2. 一个最小 Native/Runtime Smoke。

正式 Windows 发布使用：

```powershell
.\publish.ps1 -OcctRoot "D:\tools\occt-vc144-64" -Zip
```

发布会执行 Release gate、生成 Portable SDK，并在隔离环境中运行一次 .NET 10 package smoke。

## Linux

```bash
./build.sh build Release
./build.sh test Release
./build.sh smoke Release
```
