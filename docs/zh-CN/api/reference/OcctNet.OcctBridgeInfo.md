# OcctBridgeInfo

- **程序集:** `OcctNet.dll`
- **命名空间:** `OcctNet`

## 声明

```csharp
public static class OcctBridgeInfo
```

## 说明

Identifies the managed wrapper and validates the ABI of the loaded native bridge.

## 构造函数

无

## 属性

### `BuildInfo`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public string BuildInfo { get; }
```

### `NativeAbiVersion`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public int NativeAbiVersion { get; }
```

### `NativeVersion`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public string NativeVersion { get; }
```

### `OcctVersion`

公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

```csharp
public string OcctVersion { get; }
```

## 事件

无

## 方法

无

## 字段 / 枚举值

- `ExpectedAbiVersion` — `int` = 4 — 公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。
- `ManagedVersion` — `string` = 2.6.0 — 公开 API 成员。精确参数、返回类型和可用 XML Documentation 见本节。

