# 快速开始

`main` 是 Windows x64 版本，只包含 Core、WinForms 与 WPF；Avalonia 位于独立 `avalonia` 分支。

## 环境

- Windows x64
- Visual Studio 2022 C++ Toolchain
- .NET SDK 10.0.302
- OCCT 7.9.0
- CMake 3.21+

默认 OCCT SDK：

```text
D:\tools\occt-vc144-64
```

## 构建

```powershell
.\build.ps1 validate Release
.\build.ps1 managed Release
.\build.ps1 test Release
.\build.ps1 all Release -OcctRoot "D:\tools\occt-vc144-64"
.\build.ps1 smoke Release -OcctRoot "D:\tools\occt-vc144-64"
```

## 最小建模示例

```csharp
using OcctNet;

using var model = new OcctModelingSession();
var box = model.MakeBox(100, 80, 10);
var hole = model.MakeCylinder(new OcctPoint3d(50, 40, -5), OcctVector3d.UnitZ, 8, 20);
var result = model.Cut(box, hole);
model.ExportStep(result.Shape, "plate.step");
```

需要 Avalonia/跨平台时直接使用 `avalonia` 分支，不存在从 main 同步 Avalonia SDK 的流程。