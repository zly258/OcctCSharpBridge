# OcctSelectionOperation

- **Assembly:** `OcctNet.dll`
- **Namespace:** `OcctNet`

## Declaration

```csharp
public enum OcctSelectionOperation
```

## Description

Public API type. See its declaration, member signatures, and conceptual documentation for ownership, lifetime, and behavioral constraints.

## Fields / Enum Values

- `Add` — `OcctSelectionOperation` = 1 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Clear` — `OcctSelectionOperation` = 4 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Remove` — `OcctSelectionOperation` = 2 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Replace` — `OcctSelectionOperation` = 0 — Public API member. Exact parameters, return type, and available XML documentation are listed below.
- `Toggle` — `OcctSelectionOperation` = 3 — Public API member. Exact parameters, return type, and available XML documentation are listed below.

